<div class="extra-pagination">
	{{pagination}}
</div>

{{#foreach posts visibility="all"}}

<article class="{{post_class}}">
	<div class="inner">
		<div class="box post-box">
			{{#if featured}}
				<span class="post-feature">
					<i class="icon icon-star">{{> "icons/icon-star"}}</i>
				</span>
			{{/if}}
			<h2 class="post-title"><a href="{{url}}"></a></h2>
			<span class="post-meta">
				{{t "By"}}
				{{authors separator=", "}}
				{{#primary_tag}}{{t "in"}} <a class="post-meta-tag" href="{{url}}">{{name}}</a>{{/primary_tag}}
			</span>
			<p class="post-excerpt">{{excerpt}}&hellip;</p>
			<span class="post-meta">
				{{t "→"}}
				<a href="{{url}}"><time datetime="{{date format='DD-MM-YYYY'}}">{{date format="DD MMM YYYY"}}</time></a>
			</span>
		</div>
	</div>
</article>

{{/foreach}}

{{pagination}}
